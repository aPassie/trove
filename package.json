{
  "name": "trove",
  "private": true,
  "workspaces": [
    "web",
    "workers/*"
  ],
  "scripts": {
    "dev": "bun --filter='*' dev",
    "build": "cd web && bun run build",
    "typecheck": "bun --filter='*' typecheck"
  }
}
