{
  "name": "trove",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "bun --filter='*' dev",
    "build": "cd web && bun run build",
    "typecheck": "bun --filter='*' typecheck"
  }
}
